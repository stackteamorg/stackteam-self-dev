<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;

class EditCategoryMiddleware
{
    public function handle(Request $request, Closure $next)
    {
        // Middleware logic for editing categories can be added here

        return $next($request);
    }
}